import React from 'react';

const MisCreaciones = () => {
   { /*const axios = require('axios'); // Importa Axios (asegúrate de que esté instalado)

const urlArchivoJSON = '../../datos.json'; // Especifica la ruta al archivo JSON local

axios.get(urlArchivoJSON)
  .then(response => {
    const creaciones = response.data; // Obtiene los datos del archivo JSON
    console.log(creaciones); // Hacer algo con los datos, como imprimirlos en la consola
  })
  .catch(error => {
    console.error('Error al obtener los datos:', error);
  });*/}

  return (
   <>
   
   </>
  );
}

export default MisCreaciones;
